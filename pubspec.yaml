name: flex_scaffold
description: FlexScaffold is an animated, responsive and adaptive Scaffold.
version: 0.0.11
homepage: https://github.com/rydmike/flexfold
environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.13.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  collection:
  flutter_test:
    sdk: flutter
  test:

flutter:
  uses-material-design: true
